name: Build NuGet Packages and normalized artifacts

on:
  workflow_call:
    outputs:
      # Output the NuGet version suffix so that the caller workflow can have the correct package version.
      nuget-version-suffix:
        description: "The NuGet version suffix to build the packages"
        value: ${{ jobs.build-nuget-packages.outputs.nuget-version-suffix }}

permissions:
  contents: read

env:
  NUGET_PACKAGES: ${{ github.workspace }}/packages
  DOTNET_CLI_TELEMETRY_OPTOUT: 1

jobs:
  build-nuget-packages:
    # In principle this job "needs: [ build, build-container ]", however, that would cause the build and build-container
    # jobs to run twice, which is not what we want. Instead, we let the "caller" job handle the dependencies.
    outputs:
      nuget-version-suffix: ${{ steps.set-nuget-version-suffix.outputs.suffix }}
    runs-on: windows-2022
    steps:

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # tag: v6.0.2
        with:
          fetch-depth: 0 # fetching all, needed to correctly calculate version

      - name: Setup .NET
        uses: actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7 # tag: v5.2.0
        with:
          dotnet-version: | 
            8.0.419
            9.0.312
            10.0.201

      - name: Check for NuGet packages cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # tag: v5.0.5
        id: nuget-cache
        with:
          key: ${{ hashFiles('**/Directory.packages.props', './build/LibraryVersions.g.cs', '**/packages.config' ) }}
          path: ${{ env.NUGET_PACKAGES }}

      - name: Download Windows Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-windows-2022
          path: bin/ci-artifacts/bin-windows-2022

      - name: Download Ubuntu x64 Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-ubuntu-22.04
          path: bin/ci-artifacts/bin-ubuntu-22.04

      - name: Download Ubuntu arm64 Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-ubuntu-22.04-arm
          path: bin/ci-artifacts/bin-ubuntu-22.04-arm

      - name: Download Alpine x64 Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-alpine-x64
          path: bin/ci-artifacts/bin-alpine-x64

      - name: Download Alpine arm64 Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-alpine-arm64
          path: bin/ci-artifacts/bin-alpine-arm64

      - name: Download Mac-OS Artifacts from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # tag: v8.0.1
        with:
          name: bin-macos-14
          path: bin/ci-artifacts/bin-macos-14

      - name: Normalize managed binaries for packaging
        shell: pwsh
        run: |
          $managedSource = "bin/ci-artifacts/bin-windows-2022/net"
          if (-not (Test-Path $managedSource)) {
            throw "Canonical managed binary source not found: $managedSource"
          }

          $releaseArtifacts = @(
            "bin-alpine-x64",
            "bin-alpine-arm64",
            "bin-ubuntu-22.04",
            "bin-ubuntu-22.04-arm",
            "bin-macos-14"
          )

          foreach ($artifact in $releaseArtifacts) {
            $targetNet = "bin/ci-artifacts/$artifact/net"
            if (-not (Test-Path $targetNet)) {
              throw "Expected managed folder not found: $targetNet"
            }

            Remove-Item $targetNet -Recurse -Force
            New-Item -ItemType Directory -Path $targetNet -Force | Out-Null
            Copy-Item -Path (Join-Path $managedSource "*") -Destination $targetNet -Recurse -Force
          }

      - name: Build NuGet packages
        run: ./build.cmd BuildNuGetPackages

      - name: Install dotnet-validate
        run: dotnet tool install --global dotnet-validate --version 0.0.1-preview.537

      - name: Install Meziantou.Framework.NuGetPackageValidation.Tool
        run: dotnet tool install --global Meziantou.Framework.NuGetPackageValidation.Tool --version 1.0.30

      - name: Validate NuGet packages
        shell: pwsh
        run: |
          foreach ($file in (Get-ChildItem bin/nuget-artifacts//*.nupkg)) {
            dotnet validate package local $($file)
            if (-not ($LASTEXITCODE -eq 0)) {
              throw "dotnet validate failed for $($file)";
            }
            meziantou.validate-nuget-package $($file)
            if (-not ($LASTEXITCODE -eq 0)) {
              throw "meziantou.validate-nuget-package validate failed for $($file)";
            }
          }
        
      - name: Upload NuGet Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-nuget-packages
          path: bin/nuget-artifacts/

      - name: Upload normalized Windows release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-windows-2022-normalized
          path: bin/ci-artifacts/bin-windows-2022/*

      - name: Upload normalized Ubuntu x64 release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-ubuntu-22.04-normalized
          path: bin/ci-artifacts/bin-ubuntu-22.04/*

      - name: Upload normalized Ubuntu arm64 release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-ubuntu-22.04-arm-normalized
          path: bin/ci-artifacts/bin-ubuntu-22.04-arm/*

      - name: Upload normalized Alpine x64 release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-alpine-x64-normalized
          path: bin/ci-artifacts/bin-alpine-x64/*

      - name: Upload normalized Alpine arm64 release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-alpine-arm64-normalized
          path: bin/ci-artifacts/bin-alpine-arm64/*

      - name: Upload normalized macOS release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # tag: v7.0.1
        with:
          name: bin-macos-14-normalized
          path: bin/ci-artifacts/bin-macos-14/*
