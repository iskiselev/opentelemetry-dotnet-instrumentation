name: Run Command in Environment
description: Run a shell command on the host or inside an existing Docker container.

inputs:
  execution-mode:
    description: Either `container` or a shell name. If empty, defaults to `container` when container-name is set, otherwise uses the platform default shell.
    required: false
    default: ""
  container-name:
    description: Running Docker container name to exec into.
    required: false
    default: ""
  run:
    description: Shell command to execute.
    required: true
  container-publish-env:
    description: Multiline list of environment variable names to publish into the container process.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Validate execution inputs
      shell: pwsh
      run: |
        $executionMode = '${{ inputs.execution-mode }}'.Trim()
        $containerName = '${{ inputs.container-name }}'.Trim()

        if ($executionMode -eq 'container' -and [string]::IsNullOrWhiteSpace($containerName)) {
          throw 'container-name input is required when execution-mode is container.'
        }

        if (-not [string]::IsNullOrWhiteSpace($containerName) -and -not [string]::IsNullOrWhiteSpace($executionMode) -and $executionMode -ne 'container') {
          throw 'container-name cannot be combined with a non-container execution-mode.'
        }

    - name: Run on host with implicit shell
      if: ${{ inputs.execution-mode == '' && inputs.container-name == '' }}
      run: ${{ inputs.run }}

    - name: Run on host with explicit shell
      if: ${{ inputs.execution-mode != '' && inputs.execution-mode != 'container' }}
      shell: ${{ inputs.execution-mode }}
      run: ${{ inputs.run }}

    - name: Run in container
      if: ${{ inputs.execution-mode == 'container' || (inputs.execution-mode == '' && inputs.container-name != '') }}
      shell: bash
      run: |
        set -u

        docker_args=(exec -i)

        while IFS= read -r environment_name; do
          environment_name="$(printf '%s' "${environment_name}" | xargs)"
          [[ -z "${environment_name}" ]] && continue

          if [[ ! "${environment_name}" =~ ^[A-Za-z_][A-Za-z0-9_]*$ ]]; then
            echo "Invalid environment variable name: ${environment_name}" >&2
            exit 1
          fi

          docker_args+=(-e "${environment_name}")
        done <<'EOF'
        ${{ inputs.container-publish-env }}
        EOF

        docker_args+=("${{ inputs.container-name }}" /bin/sh)

        docker "${docker_args[@]}" <<'__RUN_COMMAND__'
        set -e
        ${{ inputs.run }}
        __RUN_COMMAND__
